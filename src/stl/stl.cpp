#include <string>
#include <algorithm>
#include <functional>
#include "gtest/gtest.h"

using namespace std;

class stlT : public ::testing::Test {
protected:
    stlT() {
    }
    ~stlT() override {
    }
    void SetUp() override {
    }
    void TearDown() override {
    }
};
