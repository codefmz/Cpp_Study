file(GLOB SRC "./*.cpp")
add_library(regexT STATIC ${SRC})
target_link_libraries(regexT gtest)
target_include_directories(regexT PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
target_compile_definitions(regexT PUBLIC REGEX_SRC_DIR="${CMAKE_CURRENT_SOURCE_DIR}")